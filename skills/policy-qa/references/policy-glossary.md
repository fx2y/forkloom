# Policy Glossary

- Control objective: outcome the policy must guarantee.
- Citation: span-backed evidence tuple (`docSha`, `parseId`, `page`, `chunkId`).
- Delta checklist: actions needed to move from current to required policy state.
