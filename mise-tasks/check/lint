#!/usr/bin/env bash
set -euo pipefail
source "$(git rev-parse --show-toplevel)/scripts/lib/common.sh"

cd_repo_root
require_cmds pnpm

pnpm exec biome check src scripts tests apps/api apps/web/src apps/web/test packages
write_mark "check:lint"
echo "check:lint ok"
