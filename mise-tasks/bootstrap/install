#!/usr/bin/env bash
set -euo pipefail
source "$(git rev-parse --show-toplevel)/scripts/lib/common.sh"

cd_repo_root
require_cmds mise

mise trust
mise install
MISE_EXPERIMENTAL=1 mise prep

write_mark "bootstrap:install"
echo "bootstrap:install ok"
