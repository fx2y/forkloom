#!/usr/bin/env bash
set -euo pipefail
source "$(git rev-parse --show-toplevel)/scripts/lib/common.sh"

cd_repo_root
require_cmds pnpm

mkdir -p .cache/bench
pnpm exec tsx scripts/harness/run-bench-live.ts cost

write_mark "bench:cost"
echo "bench:cost ok"
