#!/usr/bin/env bash
set -euo pipefail
source "$(git rev-parse --show-toplevel)/scripts/lib/common.sh"

cd_repo_root
require_cmds pnpm docker

mkdir -p .cache/bench
docker compose restart api >/dev/null
wait_for_url "api" "http://127.0.0.1:8080/health" /tmp/svc.api
pnpm exec tsx scripts/harness/run-bench-live.ts latency

write_mark "bench:latency"
echo "bench:latency ok"
