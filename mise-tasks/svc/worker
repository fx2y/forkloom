#!/usr/bin/env bash
set -euo pipefail
source "$(git rev-parse --show-toplevel)/scripts/lib/common.sh"

cd_repo_root
require_cmds docker

docker compose --profile worker up -d worker
write_mark "svc:worker"
echo "svc:worker ok"
