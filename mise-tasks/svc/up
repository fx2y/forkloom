#!/usr/bin/env bash
set -euo pipefail
source "$(git rev-parse --show-toplevel)/scripts/lib/common.sh"

cd_repo_root
require_cmds docker

docker compose up -d postgres seaweedfs
write_mark "svc:up"
echo "svc:up ok"
